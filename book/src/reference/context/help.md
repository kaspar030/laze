# help

This field contains an optional _string_ with help text for the context.

Example:

```yaml
contexts:
  - name: nrf52840
    parent: cortex-m4
    help: "Nordic nRF52840 SoC with BLE support"
```
